# Hospital Management Backend

A Spring Boot application built using modern Java and Maven conventions. This project provides a clean foundation for building robust, production-ready applications.

## 🚀 Tech Stack

- **Java 17**  
  The application is built using Java 17, providing access to modern language features and long-term support.

- **Spring Boot 3.5.7**  
  Leveraging the Spring Boot 3.x ecosystem for streamlined configuration and rapid development.

- **Maven**  
  Maven is used as the build and dependency management tool.

- **JAR Packaging**  
  The application is packaged as an executable `.jar` for easy deployment.

- **`.properties` Configuration**  
  Spring Boot configuration is managed using `application.properties` files.

---

## 📦 Project Structure

Typical structure (auto-generated by Spring Initializr or Maven structure):

```
Project-name
│ ├── service-name
│ ├── src
│ ├── main
│ │ ├── java
│ │ └── resources
│ │ └── application.properties
│ │ └──pom.xml
└── README.md
```


---

## ▶️ Running the Application

Make sure you have **Java 17** installed.

### 1. Build the project:

```bash
mvn clean install

```

### 2. Run the Application

```bash
java -jar target/project-name-0.0.1-SNAPSHOT.jar
```

---

## ⚙️ Configuration

Application configuration is handled via:

```
src/main/resources/application.properties
```

You can configure:

- Server port  
- Database settings  
- Logging  
- Custom properties  

---

## 📄 License

Add your license information here (MIT, Apache 2.0, GPL, etc.).
